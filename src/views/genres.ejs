<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  
  <ul>
    <% genres.forEach(genre => { %>
      <li><a href="/genres/<%= genre.category %>"><%= genre.category %></a></li>
    <% }); %>
  </ul>
</body>
</html>